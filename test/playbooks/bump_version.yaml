---
- name: Bump the version number

  tasks:
  - name: Verify pubspec version
    wings.version:
      action: verify
      pubspecPath: test/pubspecs/test1_pubspec.yaml
  - name:
    wings.shell:
      cmd: ls
