---
- name: Set version to a specific version

  tasks:
  - name: Verify pubspec version
    wings.version:
      action: verify
      pubspecPath: test/pubspecs/test1_pubspec.yaml
  - name:
    wings.shell:
      cmd: ls
